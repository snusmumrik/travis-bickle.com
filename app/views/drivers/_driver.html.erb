<tr>
  <td><%= link_to driver.name, driver_path(driver) %></td>
  <td><%= driver.email %></td>
  <td><%= link_to t("link.show"), "#{driver_path(driver)}/#{Date.today.year}/#{Date.today.month}" %></td>
  <td><%= link_to t("link.edit"), edit_driver_path(driver) %></td>
  <td><%= link_to t("link.destroy"), driver, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
