<h1><%= t("activerecord.models.driver") %><%= t("link.edit") %></h1>

<%= render 'form' %>

<%= link_to t("link.show"), @driver, :class => "btn" %>
<%= link_to t("link.back"), drivers_path, :class => "btn" %>
