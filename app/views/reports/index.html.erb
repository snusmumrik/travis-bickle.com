<h1>
  <% if params[:day] %>
    <%= "#{params[:year]}年#{params[:month]}月#{params[:day]}日" %>
  <% else %>
    <%= "#{params[:year]}年#{params[:month]}月" %>
  <% end %>
  <%= t("activerecord.models.report") %><%= t("link.report.index") %>
</h1>

<%= render "reports" %>

<%= link_to t("link.new"), new_report_path, :class => "btn" %>
