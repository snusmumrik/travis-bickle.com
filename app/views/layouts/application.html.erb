<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <title>Rails3</title>
    <meta name="description" content="">
    <meta name="author" content="">
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le styles -->
    <!-- <link href="/assets/bootstrap/css/bootstrap.css" rel="stylesheet"> -->
    <style type="text/css">
      body {
      /* 60px to make the container go all the way to the bottom of the topbar */
      padding-top: 60px;
      padding-bottom: 40px;
      }
    </style>
    <!-- <link href="/assets/bootstrap/css/bootstrap-responsive.css" rel="stylesheet"> -->

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="images/favicon.ico">
    <link rel="apple-touch-icon" href="images/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="72x72" href="images/apple-touch-icon-72x72.png">
    <link rel="apple-touch-icon" sizes="114x114" href="images/apple-touch-icon-114x114.png">

    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="i-bar"></span>
            <span class="i-bar"></span>
            <span class="i-bar"></span>
          </a>
          <a class="brand" href="/">Project name</a>
          <div class="nav-collapse">
            <ul class="nav">
              <% unless params[:id] %><li class="active"><% else %><li><% end %><%= link_to t("home"), root_path %></li>
              <% if params[:id] == "about" %><li class="active"><% else %><li><% end %><%= link_to t("about"), "/fluid" %></li>
              <% if params[:id] == "contact" %><li class="active"><% else %><li><% end %><%= link_to t("contact"), "/fluid" %></li>
            </ul>
            <% if user_signed_in? %>
              <p class="navbar-text pull-right">Logged in as <%= link_to "username", user_path(current_user.username) %></a>
              <p class="navbar-text pull-right"><%= link_to(t("link.user.signout"), destroy_user_session_path, :method => :delete) %></p>
            <% else %>
              <p class="navbar-text pull-right"><%= link_to t("link.user.signup"), new_user_registration_path %></p>
              <p class="navbar-text pull-right"><%= link_to t("link.user.signin"), new_user_session_path %></p>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <%= yield %>

      <hr>
      <footer>
        <p>&copy; <%= Date.today.year %></p>
      </footer>
    </div> <!-- /container -->

    <%= javascript_include_tag "application" %>
  </body>
</html>
