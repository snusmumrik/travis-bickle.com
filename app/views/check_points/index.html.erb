<h1><%= t("activerecord.models.check_point") %><%= t("link.index") %></h1>

<%= form_for CheckPoint.new, :url => check_points_path, :remote => true, :html => {:method => :get} do |f| %>
  <%= f.search_field :name %>
  <%= f.submit t("link.search"), :class => "btn" %>
<% end %>

<%= paginate @check_points %>
<table class="table table-striped table-bordered table-hover">
  <tr>
    <th><%=t "activerecord.attributes.check_point.name" %></th>
    <!-- <th></th> -->
    <th></th>
    <th></th>
  </tr>

  <tbody id="check_points">
    <%= render @check_points %>
  </tbody>
</table>

<br />
<%= paginate @check_points %>

<%= link_to t("link.new") + t("activerecord.models.check_point"), new_check_point_path, :class => "btn" %>
