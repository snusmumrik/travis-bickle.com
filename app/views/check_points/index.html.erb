<h1><%= t("activerecord.models.check_point") %><%= t("link.index") %></h1>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th><%=t "activerecord.attributes.check_point.name" %></th>
    <!-- <th></th> -->
    <th></th>
    <th></th>
  </tr>

<% @check_points.each do |check_point| %>
  <tr>
    <td><%= check_point.name %></td>
    <!-- <td><%#= link_to t("link.show"), check_point %></td> -->
    <td><%= link_to t("link.edit"), edit_check_point_path(check_point) %></td>
    <td><%= link_to t("link.destroy"), check_point, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t("link.new"), new_check_point_path, :class => "btn" %>
