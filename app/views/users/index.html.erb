<h1><%=t "activerecord.models.user" %></h1>

<table>
  <tr>
    <th><%=t "activerecord.attributes.user.username" %></th>
    <th><%=t "activerecord.attributes.user.image" %></th>
    <th><%=t "activerecord.attributes.user.email" %></th>
    <th></th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= image_tag user.images.pop.image.url(:thumb) %></td>
      <td><%= user.email %></td>
      <td><%= link_to t("link.edit"), edit_user_path(user.username) %></td>
      <td><%= link_to t("link.destroy"), user.username, confirm: 'Are you sure?', method: :delete %></td>
    </tr>
  <% end %>
</table>
