<h1><%= t("activerecord.models.notification") %><%= t("link.edit") %></h1>

<%= render 'form' %>

<%= link_to t("link.show"), @notification, :class => "btn" %>
<%= link_to t("link.back"), :back, :class => "btn" %>
